# UX 交互体验打磨 实现计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 为 CoinSage 添加 Toast 通知、骨架屏、响应式行操作、空状态引导和键盘快捷键，提升桌面+手机浏览器的操作体验。

**Architecture:** 以 ToastContext 全局状态管理通知，CSS `@media (hover: hover)` 区分桌面/手机行操作，骨架屏直接在各页面 loading state 中内联渲染。

**Tech Stack:** React 19, Vite, 纯 CSS（CSS 变量 + media query），无新依赖

---

## 项目现状速览

- `frontend/src/index.css` — 已有 `.skeleton` shimmer 动画、`var(--c-*)` 颜色 token、深色模式
- `frontend/src/App.jsx` — 顶部 Header + Tab Bar + FAB，useAuth 认证守卫
- `frontend/src/pages/Home.jsx` — 今日概览卡 + CategoryColumn（已有编辑/删除图标）
- `frontend/src/pages/History.jsx` — 月度账单列表（无编辑/删除）
- `frontend/src/pages/Analysis.jsx` — AI 分析，结果显示时无引导
- `frontend/src/main.jsx` — 已包裹 AuthProvider

---

### Task 1: Toast 系统 — Context + 组件 + CSS

**Files:**
- Create: `frontend/src/ToastContext.jsx`
- Create: `frontend/src/components/Toast.jsx`
- Modify: `frontend/src/index.css`（追加动画和样式）
- Modify: `frontend/src/main.jsx`（包裹 ToastProvider）

**Step 1: 新建 `frontend/src/ToastContext.jsx`**

```jsx
import { createContext, useContext, useState, useCallback } from "react"
import { ToastContainer } from "./components/Toast"

const ToastCtx = createContext(null)

export function useToast() {
  return useContext(ToastCtx)
}

export function ToastProvider({ children }) {
  const [toasts, setToasts] = useState([])

  const showToast = useCallback((message, type = "success", duration = 3000) => {
    const id = Date.now() + Math.random()
    setToasts(prev => [...prev.slice(-4), { id, message, type }])
    setTimeout(() => setToasts(prev => prev.filter(t => t.id !== id)), duration)
  }, [])

  const dismiss = useCallback((id) => {
    setToasts(prev => prev.filter(t => t.id !== id))
  }, [])

  return (
    <ToastCtx.Provider value={showToast}>
      {children}
      <ToastContainer toasts={toasts} onDismiss={dismiss} />
    </ToastCtx.Provider>
  )
}
```

**Step 2: 新建 `frontend/src/components/Toast.jsx`**

先创建目录：`mkdir -p frontend/src/components`

```jsx
const COLORS = {
  success: { bg: "var(--c-green)",  text: "#fff" },
  info:    { bg: "var(--c-blue)",   text: "#fff" },
  error:   { bg: "var(--c-red)",    text: "#fff" },
}

function ToastItem({ toast, onDismiss }) {
  const { bg, text } = COLORS[toast.type] || COLORS.info
  return (
    <div className="toast-item" style={{ background: bg, color: text }}>
      <span style={{ flex: 1, fontSize: "13px", fontWeight: 600 }}>{toast.message}</span>
      <button
        onClick={() => onDismiss(toast.id)}
        style={{ background: "none", border: "none", cursor: "pointer",
                 color: "rgba(255,255,255,0.75)", fontSize: "16px", padding: "0 0 0 10px",
                 lineHeight: 1, flexShrink: 0 }}
        aria-label="关闭"
      >✕</button>
    </div>
  )
}

export function ToastContainer({ toasts, onDismiss }) {
  if (!toasts.length) return null
  return (
    <div style={{
      position: "fixed",
      top: "calc(env(safe-area-inset-top, 0px) + 68px)",
      right: "16px",
      zIndex: 9999,
      display: "flex", flexDirection: "column", gap: "8px",
      pointerEvents: "none",
    }}>
      {toasts.map(t => (
        <div key={t.id} style={{ pointerEvents: "auto" }}>
          <ToastItem toast={t} onDismiss={onDismiss} />
        </div>
      ))}
    </div>
  )
}
```

**Step 3: 在 `frontend/src/index.css` 末尾追加 Toast 动画和行操作 CSS**

```css
/* ─── Toast ─── */
@keyframes toastIn {
  from { opacity: 0; transform: translateX(110%); }
  to   { opacity: 1; transform: translateX(0); }
}
.toast-item {
  display: flex; align-items: center;
  padding: 10px 14px; border-radius: 12px;
  box-shadow: 0 4px 16px rgba(0,0,0,0.20);
  min-width: 200px; max-width: 320px;
  animation: toastIn 0.25s cubic-bezier(0.34,1.56,0.64,1) both;
}

/* ─── Responsive Row Actions ─── */
.tx-row { transition: background 0.12s; border-radius: var(--r-sm); }

@media (hover: hover) {
  .row-actions { opacity: 0; transition: opacity 0.15s; }
  .tx-row:hover .row-actions { opacity: 1; }
  .tx-row:hover { background: var(--c-fill-2); }
}
@media (hover: none) {
  .row-actions { opacity: 1; }
}
```

**Step 4: 修改 `frontend/src/main.jsx`，用 ToastProvider 包裹**

```jsx
import { ToastProvider } from './ToastContext.jsx'

createRoot(document.getElementById('root')).render(
  <StrictMode>
    <AuthProvider>
      <ToastProvider>
        <App />
      </ToastProvider>
    </AuthProvider>
  </StrictMode>,
)
```

**Step 5: 启动前端验证**

```bash
cd frontend && npm run dev
```

打开页面，无报错即可。

**Step 6: Commit**

```bash
git add frontend/src/ToastContext.jsx frontend/src/components/Toast.jsx frontend/src/index.css frontend/src/main.jsx
git commit -m "feat: add Toast notification system"
```

---

### Task 2: 在 App.jsx 加键盘快捷键 N

**Files:**
- Modify: `frontend/src/App.jsx`

**Step 1: 在 App 组件里加 useEffect 监听键盘**

在 `App.jsx` 的 `export default function App()` 内，`useState` 声明之后，添加：

```jsx
import { useEffect } from "react"   // 顶部 import 补上

// 在函数体内，handleSaved 之后添加:
useEffect(() => {
  function onKey(e) {
    if (e.key !== "n" && e.key !== "N") return
    const tag = document.activeElement?.tagName
    if (tag === "INPUT" || tag === "TEXTAREA" || tag === "SELECT") return
    setShowAdd(true)
  }
  window.addEventListener("keydown", onKey)
  return () => window.removeEventListener("keydown", onKey)
}, [])
```

**Step 2: 验证**

浏览器打开，在非输入框状态下按 `N` → AddRecord 弹窗打开；在金额输入框内按 `N` → 不触发。

**Step 3: Commit**

```bash
git add frontend/src/App.jsx
git commit -m "feat: keyboard shortcut N to open AddRecord"
```

---

### Task 3: Toast 集成到现有操作

**Files:**
- Modify: `frontend/src/pages/AddRecord.jsx`
- Modify: `frontend/src/pages/Budget.jsx`
- Modify: `frontend/src/pages/Categories.jsx`
- Modify: `frontend/src/pages/Home.jsx`（删除操作）

在每个文件顶部添加：
```jsx
import { useToast } from "../ToastContext"
// 函数体内：
const showToast = useToast()
```

**AddRecord.jsx** — 保存成功后：
```jsx
// handleSave 里，onSaved() 之前
showToast(isEdit ? "记录已更新" : "记录已保存")
```

**Budget.jsx** — 全局预算保存后：
```jsx
// handleSave 里，setSaved(true) 之后
showToast("预算已保存")
```
分类预算保存后：
```jsx
// saveCatBudget 里，setCatSaved 之后
showToast("分类预算已保存")
```

**Categories.jsx** — 三个操作：
```jsx
// 添加成功后
showToast("分类已添加")
// 编辑成功后
showToast("分类已更新")
// 删除成功后
showToast("分类已删除")
// 删除失败时
showToast("删除失败", "error")
```

**Home.jsx** — handleDelete 成功后：
```jsx
// handleDelete 里，loadData() 之后
showToast("记录已删除")
```

**Step: Commit**

```bash
git add frontend/src/pages/AddRecord.jsx frontend/src/pages/Budget.jsx frontend/src/pages/Categories.jsx frontend/src/pages/Home.jsx
git commit -m "feat: integrate Toast into record/budget/category operations"
```

---

### Task 4: 骨架屏 — Home 页

**Files:**
- Modify: `frontend/src/pages/Home.jsx`

**Step 1: 在 Home.jsx 内新增骨架屏组件**

在 `CategoryGrid` 组件定义之前添加：

```jsx
function HomeSkeleton() {
  const sk = { borderRadius: "var(--r-lg)", background: "var(--c-fill)" }
  return (
    <div>
      {/* 顶部三卡骨架 */}
      <div className="bento" style={{ marginBottom: "20px" }}>
        <div className="skeleton bento-2" style={{ ...sk, height: "160px" }} />
        <div className="skeleton" style={{ ...sk, height: "160px" }} />
        <div className="skeleton" style={{ ...sk, height: "160px" }} />
      </div>
      {/* 分类列骨架 */}
      <div style={{ height: "20px", width: "120px", borderRadius: "6px" }}
           className="skeleton" />
      <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fill,minmax(170px,1fr))",
                    gap: "12px", marginTop: "12px" }}>
        {[...Array(4)].map((_, i) => (
          <div key={i} className="skeleton" style={{ ...sk, height: "200px" }} />
        ))}
      </div>
    </div>
  )
}
```

**Step 2: 在 Home 函数体内用 `loadingData` state 控制**

```jsx
const [loadingData, setLoadingData] = useState(true)

// loadData 函数改为：
async function loadData() {
  setLoadingData(true)
  try {
    // ... 现有代码不变 ...
  } finally {
    setLoadingData(false)
  }
}
```

**Step 3: 在 return 开头加条件**

```jsx
if (loadingData && !budget) return <HomeSkeleton />
```

放在 `return (` 之前（第一次加载时显示骨架，有数据后不再显示）。

**Step 4: Commit**

```bash
git add frontend/src/pages/Home.jsx
git commit -m "feat: skeleton loading for Home page"
```

---

### Task 5: 骨架屏 + 空状态 — History 页

**Files:**
- Modify: `frontend/src/pages/History.jsx`

**Step 1: 替换现有 loading 占位为骨架屏**

找到现有：
```jsx
{loading ? (
  <div style={{ textAlign: "center", padding: "60px 0", color: "var(--c-text-3)", fontSize: "14px" }}>加载中…</div>
) :
```

替换 `<div>加载中…</div>` 为：
```jsx
<div style={{ display: "flex", flexDirection: "column", gap: "12px" }}>
  {[...Array(3)].map((_, i) => (
    <div key={i} className="skeleton" style={{ height: "120px", borderRadius: "var(--r-lg)" }} />
  ))}
</div>
```

**Step 2: 空状态已存在，确认样式**

History.jsx 已有 `sortedDates.length === 0` 的空状态（📭 图标），无需改动。

**Step 3: Commit**

```bash
git add frontend/src/pages/History.jsx
git commit -m "feat: skeleton loading for History page"
```

---

### Task 6: 空状态 — Home 今日分类 + Analysis 引导

**Files:**
- Modify: `frontend/src/pages/Home.jsx`
- Modify: `frontend/src/pages/Analysis.jsx`

**Home.jsx — 今日无记录时的空状态**

在 `CategoryGrid` 组件内，找到渲染 `categories.map(...)` 的部分。

在 CategoryGrid 的 return 中，在 categories 列表之前（或当 transactions 为空且 categories 有数据时）展示引导：

在 `Home` 函数的 return 中，`<CategoryGrid .../>` 下方找到结构，在 `CategoryGrid` 的 `return` 里（grid div 内部）添加一个顶部整体空状态。更简单的方案：在 `CategoryGrid` 外判断：

```jsx
{/* 分类列 */}
{categories.length > 0 && transactions.length === 0 ? (
  <div className="card fade-up" style={{ textAlign: "center", padding: "48px 24px", animationDelay: "140ms" }}>
    <p style={{ fontSize: "36px", marginBottom: "12px" }}>📋</p>
    <p style={{ fontSize: "15px", fontWeight: 600, color: "var(--c-text-1)", marginBottom: "6px" }}>
      今天还没有支出
    </p>
    <p style={{ fontSize: "13px", color: "var(--c-text-3)", marginBottom: "20px" }}>
      点击右下角 + 记录第一笔，或按 <kbd style={{
        background: "var(--c-fill)", padding: "1px 6px", borderRadius: "4px",
        fontSize: "12px", fontFamily: "var(--font)",
      }}>N</kbd> 快速记账
    </p>
    <button className="btn-primary" onClick={onAddClick}
      style={{ maxWidth: "160px", margin: "0 auto" }}>
      ＋ 记一笔
    </button>
  </div>
) : (
  <CategoryGrid
    categories={categories}
    transactions={transactions}
    onEdit={setEditingTx}
    onDelete={handleDelete}
    monthSpentByCategory={monthSpentByCategory}
    categoryBudgetMap={categoryBudgetMap}
  />
)}
```

**Analysis.jsx — 未分析时的引导**

找到 `{result && (...)}` 结果卡片区域。在控制卡和结果卡之间，当 `!result && !loading` 时显示：

在 `analysis-grid` div 内，结果卡条件渲染之前添加：

```jsx
{!result && !loading && (
  <div className="card fade-up" style={{ padding: "40px 24px", textAlign: "center", animationDelay: "60ms" }}>
    <p style={{ fontSize: "32px", marginBottom: "12px" }}>✨</p>
    <p style={{ fontSize: "15px", fontWeight: 600, color: "var(--c-text-1)", marginBottom: "6px" }}>
      还没有分析结果
    </p>
    <p style={{ fontSize: "13px", color: "var(--c-text-3)" }}>
      选择分析模式，点击「开始分析」获取 AI 建议
    </p>
  </div>
)}
```

**Step: Commit**

```bash
git add frontend/src/pages/Home.jsx frontend/src/pages/Analysis.jsx
git commit -m "feat: empty state guides for Home and Analysis pages"
```

---

### Task 7: 响应式行操作 — Home CategoryColumn

**Files:**
- Modify: `frontend/src/pages/Home.jsx`

CSS 已在 Task 1 中添加（`.tx-row` / `.row-actions`）。现在给 Home.jsx 的交易行加类名。

**Step 1: 找到 CategoryColumn 内的交易行 div**

找到每条记录的最外层容器 div（`display: "flex", alignItems: "center", padding: "8px 14px"`）。

给它加 `className="tx-row"` 并调整内联 padding 样式：

```jsx
<div key={t.id}
  className="tx-row"
  style={{ display: "flex", alignItems: "center", padding: "8px 14px", gap: "8px" }}
>
```

**Step 2: 给操作按钮容器加 `className="row-actions"`**

找到 EditIcon 和 TrashIcon 两个按钮，将它们包裹在：

```jsx
<div className="row-actions" style={{ display: "flex", alignItems: "center", gap: "4px" }}>
  {/* EditIcon 按钮 */}
  {/* TrashIcon 按钮 */}
</div>
```

**Step 3: 验证**

- 桌面浏览器：行默认无按钮，hover 后按钮出现
- 手机浏览器（Chrome DevTools 手机模式）：按钮始终可见

**Step 4: Commit**

```bash
git add frontend/src/pages/Home.jsx
git commit -m "feat: responsive row actions on Home (hover:desktop, always:mobile)"
```

---

### Task 8: 响应式行操作 — History 页

**Files:**
- Modify: `frontend/src/pages/History.jsx`
- Modify: `frontend/src/api.js`（已有 updateTransaction，无需改动）

History 目前只展示记录，无编辑/删除。添加响应式操作按钮，并引入 Toast。

**Step 1: 在 History.jsx 顶部引入所需**

```jsx
import { useToast } from "../ToastContext"
import AddRecord from "./AddRecord"   // 复用编辑弹窗
```

**Step 2: 在 History 函数体内添加编辑状态和删除处理**

```jsx
const showToast = useToast()
const [editingTx, setEditingTx] = useState(null)

async function handleDelete(id) {
  try {
    await api.deleteTransaction(id)
    setTransactions(prev => prev.filter(t => t.id !== id))
    showToast("记录已删除")
  } catch {
    showToast("删除失败", "error")
  }
}
```

**Step 3: 给流水列表每行加 tx-row 类名和操作按钮**

找到流水列表的每条记录 div（`display: "flex", alignItems: "center", padding: "11px 16px"`）。

```jsx
<div key={t.id} className="tx-row"
  style={{ display: "flex", alignItems: "center", padding: "11px 16px", gap: "10px" }}>
  <div style={{ flex: 1, minWidth: 0 }}>
    {/* 现有内容不变 */}
  </div>
  <span style={{ fontSize: "14px", fontWeight: 600, color: "var(--c-red)",
                 fontVariantNumeric: "tabular-nums", whiteSpace: "nowrap" }}>
    −¥{fmt(t.amount)}
  </span>
  <div className="row-actions" style={{ display: "flex", gap: "4px", flexShrink: 0 }}>
    <button onClick={() => setEditingTx(t)}
      style={{ width: "28px", height: "28px", borderRadius: "8px", border: "none",
               background: "rgba(59,130,246,0.12)", cursor: "pointer",
               display: "flex", alignItems: "center", justifyContent: "center",
               color: "var(--c-blue)" }}>
      <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round">
        <path d="M11 4H4a2 2 0 00-2 2v14a2 2 0 002 2h14a2 2 0 002-2v-7"/>
        <path d="M18.5 2.5a2.121 2.121 0 013 3L12 15l-4 1 1-4 9.5-9.5z"/>
      </svg>
    </button>
    <button onClick={() => handleDelete(t.id)}
      style={{ width: "28px", height: "28px", borderRadius: "8px", border: "none",
               background: "rgba(239,68,68,0.12)", cursor: "pointer",
               display: "flex", alignItems: "center", justifyContent: "center",
               color: "var(--c-red)" }}>
      <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round">
        <polyline points="3 6 5 6 21 6"/>
        <path d="M19 6l-1 14a2 2 0 01-2 2H8a2 2 0 01-2-2L5 6"/>
        <path d="M10 11v6M14 11v6"/>
      </svg>
    </button>
  </div>
</div>
```

**Step 4: 在 History return 末尾加编辑弹窗**

在最后的 `</div>` 之前：
```jsx
{editingTx && (
  <AddRecord
    editData={editingTx}
    onClose={() => setEditingTx(null)}
    onSaved={() => {
      setEditingTx(null)
      // 重新加载当月数据
      api.getMonthTransactions(month).then(setTransactions)
      showToast("记录已更新")
    }}
  />
)}
```

**Step 5: Commit**

```bash
git add frontend/src/pages/History.jsx
git commit -m "feat: responsive row edit/delete on History page with Toast"
```

---

## 完成验证清单

```
□ 新建记录 → 右上角绿色 Toast，3 秒消失，可手动关闭
□ 删除记录 → Toast "记录已删除"
□ 保存预算 → Toast "预算已保存"
□ 首页骨架：刷新后瞬间显示占位卡，数据到来后切换
□ 账单页骨架：切换月份时显示 3 条骨架行
□ 首页今日无记录：显示空状态引导卡 + 记一笔按钮
□ 账单页无记录：显示 📅 空状态（已有，确认正常）
□ Analysis 未分析：显示 ✨ 引导文字
□ 桌面 hover：行高亮 + 按钮出现；非 hover 行按钮隐藏
□ 手机模拟器：所有行按钮始终可见
□ 按 N 键（非输入框）→ AddRecord 弹窗打开
□ 在金额输入框内按 N → 不触发弹窗
```
